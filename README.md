# GoL
Game of Life
# Run
* Create a virtual environment and install the packages in the requirements file.\
`pip install --upgrade pip`\
`pip install -r requirements.txt`

* Run the game from project root\
`python game.py`
use the `--help` argument to see what type of arguments you can pass.


# Todo
 * The grid should have a bigger boundary than the starting positions to let the patterns grow
 * Should make a UI to show grid in a neater way
 
